apiVersion: v1
kind: Service
metadata:
  name: redis-master-svc
  labels:
    app: redis
    role: master
    tier: backend
spec:
  type: ClusterIP
  ports:
  - targetPort: 6379
    port: 6379
  selector:
    app: redis
    role: master
    tier: backend